// While Loop :
//          It is the loop , the loop will continue to runs the function until the condition given inn the loop is get failed.

// Syntax :
            while(condition){

            }

// For Example :
            var a = [];
            let n = 1;
            while(n <= 234){
                a.push(n);
                console.log(a)
                n++;
            }   