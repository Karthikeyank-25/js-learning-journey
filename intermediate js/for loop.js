// For Loop :
//      It is the loop , in this loop all the condition are predefined like (start,end,wise) while declering a loop.

// Syntax :
        for(i=1;i<n;i++){
            
        }

// For Example :
        let tickBox = [];
        for(let i=1;i<=1000;i++){
                tickBox.push(i);
        }
        console.log(tickBox);