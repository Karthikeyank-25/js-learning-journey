// Random Number Generation :
//          It is used to generate random number from within 0 to 1.

// Syntax :
            Math.random();

// For Example :
            var a = Math.random();
            console.log(a);
