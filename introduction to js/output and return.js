// Output and Return :
//      - Output , We cannot use the output in another variable or anything.
//      - But return , it makes the functions output into a usable value programmatically not only for displaying a value.
//        it returning values and outputs from functions.

// Syntax :
        function drivingJob(start,end){
            let a = "The trip starts from "+start+" and ends in "+end+".";  // returning functions output
            return a;
        }
        drivingJob("Tiruvannamalai","Chennai");                             // calling function and returning output