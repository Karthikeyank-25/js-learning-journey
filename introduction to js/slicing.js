// Slicing :
//      It is used to slice and separate the words by taking inputs from us where to begins the slice and the end to stop the slice.

// Syntax :
        var typeName = "String";
        typeName.slice(2,4);

// For Example :
        var countryName = "India";
        countryName.slice(0,3);